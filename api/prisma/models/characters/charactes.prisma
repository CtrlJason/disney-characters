model Character {
    id          Int      @id @default(autoincrement())
    name        String
    date        DateTime
    description String
    imageUrl    String
    films       String[]

    createAt DateTime @default(now())
    updateAt DateTime @updatedAt
}

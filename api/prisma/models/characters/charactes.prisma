model Character {
    id          Int      @id @default(autoincrement())
    name        String
    date        DateTime
    description String
    image       String

    createAt DateTime @default(now())
    updateAt DateTime @updatedAt
}
